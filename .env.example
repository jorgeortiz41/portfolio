# The companion chatbot (/api/chat). Without it the sprite still walks and the
# local quips still fire — it just answers "my brain's not plugged in" instead
# of reaching the model. The site builds and runs fine with this unset.
ANTHROPIC_API_KEY=

# Optional override. Defaults to claude-sonnet-5.
# CHAT_MODEL=claude-sonnet-5
